## Installation
run 'yarn' at the project's folder.

## Usage

Initialize: run 'yarn start'.

After initialization the server is ready.

The shared folder located at the project's folder and named 'sharedFolder' unless you have changed it.